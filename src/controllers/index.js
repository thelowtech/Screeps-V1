let controllers = {
    defense: require("controllers_defense"),
    memory: require("controllers_memory"),
    spawn: require("controllers_spawn"),
    job: require("controllers_job"),
}

module.exports = controllers;
